{
  "name": "@frontend/app",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "esbuild": "src/index.ts",
  "license": "None",
  "scripts": {
    "start": "esbuild src/index.tsx --define:process.env.NODE_ENV=\\\"development\\\" --bundle --serve --sourcemap --servedir=public --outdir=public/bundle",
    "build": "esbuild src/index.tsx --define:process.env.NODE_ENV=\\\"production\\\" --bundle --minify --outdir=public/bundle"
  },
  "dependencies": {
    "@xstate/react": "^1.5.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.14",
    "@types/react-dom": "^17.0.9"
  }
}
